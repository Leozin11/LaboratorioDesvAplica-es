import 'package:flutter/material.dart';

class ClienteHomeScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Área do Cliente')),
      body: Center(child: Text('Bem-vindo, cliente!')),
    );
  }
}
